import { combineReducers } from "redux";
import posts from "./posts";

export default combineReducers({
    posts   // posts: posts cũng được nhưng trong js key và value giống nhau thì ghi 1 đc rồi
})